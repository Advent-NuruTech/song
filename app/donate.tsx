// Keep the monthly support invitation and the permanent sidebar destination on
// one implementation so payment validation and checkout behavior cannot drift.
import DonationScreen from "./support/index";

export default DonationScreen;
